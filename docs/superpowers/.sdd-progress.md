# SDD Progress — Professional Portfolio

Plan: docs/superpowers/plans/2026-07-02-professional-portfolio.md
Constraint: NO git commands (owner commits). Reviews read files directly.

## Chunks
- [x] Chunk 1 — Foundation: Tasks 1,2,3,4 (scaffold, test harness, types, content data) — review clean
- [x] Chunk 2 — Shell: Tasks 5,6,7,8 (styles, layout/SEO, UI primitives, Navbar/Footer) — review clean + reduced-motion fix applied
- [x] Chunk 3 — Sections: Tasks 9–15 (Hero…Contact) — review approved + Projects.tsx github link now sourced from data
  - Minor findings logged for final review: array-index keys in About/Timeline; ProjectCard icons lack aria-hidden; TimelineItem left-[5px] magic value.
- [x] Chunk 4 — Assembly + build: Tasks 16,17 (page, smoke test, export, docs) — review approved
- [x] Final whole-branch review — verdict READY TO SHIP (no Critical/Important). Polish applied: aria-hidden on inline icons (ProjectCard, Achievements), dynamic footer year. All content server-rendered into static HTML. Owner action: replace placeholder résumé PDF.

## Deploy
- Target: GitHub Pages (this repo, subpath) → https://viscous106.github.io/professionalPortfolio
- Added: .github/workflows/deploy.yml (build web/ + deploy on push to main); env-driven basePath in next.config.mjs (PAGES_BASE_PATH); public/.nojekyll; lib/basePath.ts + withBasePath() for the résumé link. Verified both subpath and root builds; tests 8/8.
- Owner must: (1) get this onto `main`, (2) Settings → Pages → Source: GitHub Actions, (3) replace placeholder résumé PDF.

## Log
- Chunk 1: complete — Next 16 scaffolded (not 15, benign), tsc/test(5)/build all pass, review clean. Note: mergit/CrownOS repo links are org URLs (intentional).
- Chunk 2: complete — styles/layout/primitives/navbar/footer, review approved. Fix: Reveal.tsx now uses framer-motion useReducedMotion to honor prefers-reduced-motion. tsc+build verified.
- Chunk 3: complete — 7 sections + supporting ui, review approved. Fix: Projects.tsx github "view all" link now sourced from portfolio.socials.
- Chunk 4: complete — page.tsx assembles 7 sections; page smoke test (8/8 total) real & unweakened; build+export verified. Sound fixes: MockIntersectionObserver in vitest.setup.ts, tsconfig types:["vitest/globals"]. Owner must replace placeholder résumé PDF.
